<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!--Menu desplegable-->
    <item
        android:id="@+id/hamburger"
        android:icon="@drawable/menu"
        android:title="barra"
        app:showAsAction="ifRoom">
        <!--Items del menu-->
        <!--Se puede añadir icon a cada item con la etiqueta  android:icon-->
        <menu>
            <item
                android:id="@+id/m_home"
                android:title="Inicio"
                android:icon="@drawable/home"/>
            <item
                android:id="@+id/m_modo"
                android:icon="@drawable/mode"
                android:title="Libre"/>

            <item android:id="@+id/m_progreso"
                android:icon="@drawable/progress"
                android:title="Progreso"/>

            <item android:id="@+id/m_logout"
                android:icon="@drawable/logout"
                android:title="Logout"/>
        </menu>
    </item>
</menu>